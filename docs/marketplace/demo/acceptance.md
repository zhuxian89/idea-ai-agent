# Email validation

- Trim leading and trailing whitespace before validation.
- Accept ` hello@example.com `.
- Keep rejecting malformed email addresses.
- Add a regression test for whitespace input.

Public, synthetic sample for the Marketplace preview. No private project data.
